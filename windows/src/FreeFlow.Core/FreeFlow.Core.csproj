<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>FreeFlow.Core</RootNamespace>
    <AssemblyName>FreeFlow.Core</AssemblyName>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>
  <ItemGroup>
    <!-- Lets the test project exercise internal helpers without widening the public API. -->
    <InternalsVisibleTo Include="FreeFlow.Core.Tests" />
  </ItemGroup>
</Project>
